// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"sync"
	"time"
	"uuid"

	"github.com/lesomnus/cr/internal/ent/audit"
	"github.com/lesomnus/cr/internal/ent/holder"
	"github.com/lesomnus/cr/internal/ent/manifest"
	"github.com/lesomnus/cr/internal/ent/manifestblob"
	"github.com/lesomnus/cr/internal/ent/outbox"
	"github.com/lesomnus/cr/internal/ent/repository"
	"github.com/lesomnus/cr/internal/ent/tag"
	"github.com/lesomnus/cr/internal/ent/tenant"
	"github.com/protobuf-orm/ent"
)

const (
	// Operation types.
	OpCreate    = ent.OpCreate
	OpDelete    = ent.OpDelete
	OpDeleteOne = ent.OpDeleteOne
	OpUpdate    = ent.OpUpdate
	OpUpdateOne = ent.OpUpdateOne

	// Node types.
	TypeAudit        = "Audit"
	TypeHolder       = "Holder"
	TypeManifest     = "Manifest"
	TypeManifestBlob = "ManifestBlob"
	TypeOutbox       = "Outbox"
	TypeRepository   = "Repository"
	TypeTag          = "Tag"
	TypeTenant       = "Tenant"
)

// AuditMutation represents an operation that mutates the Audit nodes in the graph.
type AuditMutation struct {
	audit.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Audit, error)
}

var _ ent.Mutation = (*AuditMutation)(nil)

// auditOption allows management of the mutation configuration using functional options.
type auditOption func(*AuditMutation)

// newAuditMutation creates new mutation for the Audit entity.
func newAuditMutation(c config, op Op, opts ...auditOption) *AuditMutation {
	m := &AuditMutation{
		Mutation: *audit.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Audit entities.
func (m *AuditMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *AuditMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withAuditId sets the Id field of the mutation.
func withAuditId(id uuid.UUID) auditOption {
	return func(m *AuditMutation) {
		var (
			err   error
			once  sync.Once
			value *Audit
		)
		m.oldValue = func(ctx context.Context) (*Audit, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Audit.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withAudit sets the old Audit of the mutation.
func withAudit(node *Audit) auditOption {
	return func(m *AuditMutation) {
		m.oldValue = func(context.Context) (*Audit, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m AuditMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m AuditMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *AuditMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Audit.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldTenantId returns the old "tenant_id" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldTenantId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldTenantId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldTenantId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTenantId: %w", err)
	}
	return oldValue.TenantId, nil
}

// OldActorId returns the old "actor_id" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldActorId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldActorId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldActorId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldActorId: %w", err)
	}
	return oldValue.ActorId, nil
}

// OldTraceId returns the old "trace_id" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldTraceId(ctx context.Context) (v []byte, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldTraceId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldTraceId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTraceId: %w", err)
	}
	return oldValue.TraceId, nil
}

// OldAction returns the old "action" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldAction(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldAction is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldAction requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAction: %w", err)
	}
	return oldValue.Action, nil
}

// OldObjectId returns the old "object_id" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldObjectId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldObjectId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldObjectId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldObjectId: %w", err)
	}
	return oldValue.ObjectId, nil
}

// OldPatch returns the old "patch" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldPatch(ctx context.Context) (v []byte, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldPatch is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldPatch requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPatch: %w", err)
	}
	return oldValue.Patch, nil
}

// OldDateCreated returns the old "date_created" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldActorTenantId returns the old "actor_tenant_id" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldActorTenantId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldActorTenantId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldActorTenantId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldActorTenantId: %w", err)
	}
	return oldValue.ActorTenantId, nil
}

// OldValue returns the old "value" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldValue(ctx context.Context) (v []byte, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldValue is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldValue requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldValue: %w", err)
	}
	return oldValue.Value, nil
}

// OldCounterpartTenantId returns the old "counterpart_tenant_id" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldCounterpartTenantId(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldCounterpartTenantId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldCounterpartTenantId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldCounterpartTenantId: %w", err)
	}
	return oldValue.CounterpartTenantId, nil
}

// OldDomain returns the old "domain" field's value of the Audit entity.
// If the Audit object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *AuditMutation) OldDomain(ctx context.Context) (v uint32, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDomain is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDomain requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDomain: %w", err)
	}
	return oldValue.Domain, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *AuditMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case audit.FieldTenantId:
		return m.OldTenantId(ctx)
	case audit.FieldActorId:
		return m.OldActorId(ctx)
	case audit.FieldTraceId:
		return m.OldTraceId(ctx)
	case audit.FieldAction:
		return m.OldAction(ctx)
	case audit.FieldObjectId:
		return m.OldObjectId(ctx)
	case audit.FieldPatch:
		return m.OldPatch(ctx)
	case audit.FieldDateCreated:
		return m.OldDateCreated(ctx)
	case audit.FieldActorTenantId:
		return m.OldActorTenantId(ctx)
	case audit.FieldValue:
		return m.OldValue(ctx)
	case audit.FieldCounterpartTenantId:
		return m.OldCounterpartTenantId(ctx)
	case audit.FieldDomain:
		return m.OldDomain(ctx)
	}
	return nil, fmt.Errorf("unknown Audit field %s", name)
}

// HolderMutation represents an operation that mutates the Holder nodes in the graph.
type HolderMutation struct {
	holder.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Holder, error)
}

var _ ent.Mutation = (*HolderMutation)(nil)

// holderOption allows management of the mutation configuration using functional options.
type holderOption func(*HolderMutation)

// newHolderMutation creates new mutation for the Holder entity.
func newHolderMutation(c config, op Op, opts ...holderOption) *HolderMutation {
	m := &HolderMutation{
		Mutation: *holder.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Holder entities.
func (m *HolderMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *HolderMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withHolderId sets the Id field of the mutation.
func withHolderId(id uuid.UUID) holderOption {
	return func(m *HolderMutation) {
		var (
			err   error
			once  sync.Once
			value *Holder
		)
		m.oldValue = func(ctx context.Context) (*Holder, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Holder.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withHolder sets the old Holder of the mutation.
func withHolder(node *Holder) holderOption {
	return func(m *HolderMutation) {
		m.oldValue = func(context.Context) (*Holder, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m HolderMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m HolderMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *HolderMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Holder.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldAlias returns the old "alias" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldAlias(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldAlias is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldAlias requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAlias: %w", err)
	}
	return oldValue.Alias, nil
}

// OldName returns the old "name" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldName requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// OldDesc returns the old "desc" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldDesc(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDesc is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDesc requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDesc: %w", err)
	}
	return oldValue.Desc, nil
}

// OldLabels returns the old "labels" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldLabels(ctx context.Context) (v map[string]string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldLabels is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldLabels requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldLabels: %w", err)
	}
	return oldValue.Labels, nil
}

// OldDateUpdated returns the old "date_updated" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldDateUpdated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateUpdated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateUpdated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateUpdated: %w", err)
	}
	return oldValue.DateUpdated, nil
}

// OldDateErased returns the old "date_erased" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldDateErased(ctx context.Context) (v *time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateErased is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateErased requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateErased: %w", err)
	}
	return oldValue.DateErased, nil
}

// OldDateCreated returns the old "date_created" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldIdpSubject returns the old "idp_subject" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldIdpSubject(ctx context.Context) (v *string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldIdpSubject is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldIdpSubject requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldIdpSubject: %w", err)
	}
	return oldValue.IdpSubject, nil
}

// OldTenantId returns the old "tenant_id" field's value of the Holder entity.
// If the Holder object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *HolderMutation) OldTenantId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldTenantId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldTenantId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTenantId: %w", err)
	}
	return oldValue.TenantId, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *HolderMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case holder.FieldAlias:
		return m.OldAlias(ctx)
	case holder.FieldName:
		return m.OldName(ctx)
	case holder.FieldDesc:
		return m.OldDesc(ctx)
	case holder.FieldLabels:
		return m.OldLabels(ctx)
	case holder.FieldDateUpdated:
		return m.OldDateUpdated(ctx)
	case holder.FieldDateErased:
		return m.OldDateErased(ctx)
	case holder.FieldDateCreated:
		return m.OldDateCreated(ctx)
	case holder.FieldIdpSubject:
		return m.OldIdpSubject(ctx)
	case holder.FieldTenantId:
		return m.OldTenantId(ctx)
	}
	return nil, fmt.Errorf("unknown Holder field %s", name)
}

// ManifestMutation represents an operation that mutates the Manifest nodes in the graph.
type ManifestMutation struct {
	manifest.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Manifest, error)
}

var _ ent.Mutation = (*ManifestMutation)(nil)

// manifestOption allows management of the mutation configuration using functional options.
type manifestOption func(*ManifestMutation)

// newManifestMutation creates new mutation for the Manifest entity.
func newManifestMutation(c config, op Op, opts ...manifestOption) *ManifestMutation {
	m := &ManifestMutation{
		Mutation: *manifest.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Manifest entities.
func (m *ManifestMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *ManifestMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withManifestId sets the Id field of the mutation.
func withManifestId(id uuid.UUID) manifestOption {
	return func(m *ManifestMutation) {
		var (
			err   error
			once  sync.Once
			value *Manifest
		)
		m.oldValue = func(ctx context.Context) (*Manifest, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Manifest.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withManifest sets the old Manifest of the mutation.
func withManifest(node *Manifest) manifestOption {
	return func(m *ManifestMutation) {
		m.oldValue = func(context.Context) (*Manifest, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m ManifestMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m ManifestMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *ManifestMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Manifest.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldRepo returns the old "repo" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldRepo(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldRepo is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldRepo requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRepo: %w", err)
	}
	return oldValue.Repo, nil
}

// OldDigest returns the old "digest" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldDigest(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDigest is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDigest requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDigest: %w", err)
	}
	return oldValue.Digest, nil
}

// OldMediaType returns the old "media_type" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldMediaType(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldMediaType is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldMediaType requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldMediaType: %w", err)
	}
	return oldValue.MediaType, nil
}

// OldArtifactType returns the old "artifact_type" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldArtifactType(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldArtifactType is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldArtifactType requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldArtifactType: %w", err)
	}
	return oldValue.ArtifactType, nil
}

// OldSubject returns the old "subject" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldSubject(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldSubject is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldSubject requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldSubject: %w", err)
	}
	return oldValue.Subject, nil
}

// OldSize returns the old "size" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldSize(ctx context.Context) (v int64, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldSize is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldSize requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldSize: %w", err)
	}
	return oldValue.Size, nil
}

// OldAnnotations returns the old "annotations" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldAnnotations(ctx context.Context) (v map[string]string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldAnnotations is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldAnnotations requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAnnotations: %w", err)
	}
	return oldValue.Annotations, nil
}

// OldDatePulled returns the old "date_pulled" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldDatePulled(ctx context.Context) (v *time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDatePulled is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDatePulled requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDatePulled: %w", err)
	}
	return oldValue.DatePulled, nil
}

// OldDateUpdated returns the old "date_updated" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldDateUpdated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateUpdated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateUpdated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateUpdated: %w", err)
	}
	return oldValue.DateUpdated, nil
}

// OldDateCreated returns the old "date_created" field's value of the Manifest entity.
// If the Manifest object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *ManifestMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case manifest.FieldRepo:
		return m.OldRepo(ctx)
	case manifest.FieldDigest:
		return m.OldDigest(ctx)
	case manifest.FieldMediaType:
		return m.OldMediaType(ctx)
	case manifest.FieldArtifactType:
		return m.OldArtifactType(ctx)
	case manifest.FieldSubject:
		return m.OldSubject(ctx)
	case manifest.FieldSize:
		return m.OldSize(ctx)
	case manifest.FieldAnnotations:
		return m.OldAnnotations(ctx)
	case manifest.FieldDatePulled:
		return m.OldDatePulled(ctx)
	case manifest.FieldDateUpdated:
		return m.OldDateUpdated(ctx)
	case manifest.FieldDateCreated:
		return m.OldDateCreated(ctx)
	}
	return nil, fmt.Errorf("unknown Manifest field %s", name)
}

// ManifestBlobMutation represents an operation that mutates the ManifestBlob nodes in the graph.
type ManifestBlobMutation struct {
	manifestblob.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*ManifestBlob, error)
}

var _ ent.Mutation = (*ManifestBlobMutation)(nil)

// manifestblobOption allows management of the mutation configuration using functional options.
type manifestblobOption func(*ManifestBlobMutation)

// newManifestBlobMutation creates new mutation for the ManifestBlob entity.
func newManifestBlobMutation(c config, op Op, opts ...manifestblobOption) *ManifestBlobMutation {
	m := &ManifestBlobMutation{
		Mutation: *manifestblob.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of ManifestBlob entities.
func (m *ManifestBlobMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *ManifestBlobMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withManifestBlobId sets the Id field of the mutation.
func withManifestBlobId(id uuid.UUID) manifestblobOption {
	return func(m *ManifestBlobMutation) {
		var (
			err   error
			once  sync.Once
			value *ManifestBlob
		)
		m.oldValue = func(ctx context.Context) (*ManifestBlob, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().ManifestBlob.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withManifestBlob sets the old ManifestBlob of the mutation.
func withManifestBlob(node *ManifestBlob) manifestblobOption {
	return func(m *ManifestBlobMutation) {
		m.oldValue = func(context.Context) (*ManifestBlob, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m ManifestBlobMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m ManifestBlobMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *ManifestBlobMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().ManifestBlob.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldRepo returns the old "repo" field's value of the ManifestBlob entity.
// If the ManifestBlob object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestBlobMutation) OldRepo(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldRepo is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldRepo requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRepo: %w", err)
	}
	return oldValue.Repo, nil
}

// OldManifest returns the old "manifest" field's value of the ManifestBlob entity.
// If the ManifestBlob object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestBlobMutation) OldManifest(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldManifest is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldManifest requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldManifest: %w", err)
	}
	return oldValue.Manifest, nil
}

// OldBlob returns the old "blob" field's value of the ManifestBlob entity.
// If the ManifestBlob object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestBlobMutation) OldBlob(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldBlob is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldBlob requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldBlob: %w", err)
	}
	return oldValue.Blob, nil
}

// OldDateCreated returns the old "date_created" field's value of the ManifestBlob entity.
// If the ManifestBlob object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ManifestBlobMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *ManifestBlobMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case manifestblob.FieldRepo:
		return m.OldRepo(ctx)
	case manifestblob.FieldManifest:
		return m.OldManifest(ctx)
	case manifestblob.FieldBlob:
		return m.OldBlob(ctx)
	case manifestblob.FieldDateCreated:
		return m.OldDateCreated(ctx)
	}
	return nil, fmt.Errorf("unknown ManifestBlob field %s", name)
}

// OutboxMutation represents an operation that mutates the Outbox nodes in the graph.
type OutboxMutation struct {
	outbox.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Outbox, error)
}

var _ ent.Mutation = (*OutboxMutation)(nil)

// outboxOption allows management of the mutation configuration using functional options.
type outboxOption func(*OutboxMutation)

// newOutboxMutation creates new mutation for the Outbox entity.
func newOutboxMutation(c config, op Op, opts ...outboxOption) *OutboxMutation {
	m := &OutboxMutation{
		Mutation: *outbox.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Outbox entities.
func (m *OutboxMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *OutboxMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withOutboxId sets the Id field of the mutation.
func withOutboxId(id uuid.UUID) outboxOption {
	return func(m *OutboxMutation) {
		var (
			err   error
			once  sync.Once
			value *Outbox
		)
		m.oldValue = func(ctx context.Context) (*Outbox, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Outbox.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withOutbox sets the old Outbox of the mutation.
func withOutbox(node *Outbox) outboxOption {
	return func(m *OutboxMutation) {
		m.oldValue = func(context.Context) (*Outbox, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m OutboxMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m OutboxMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *OutboxMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Outbox.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldTenantId returns the old "tenant_id" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldTenantId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldTenantId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldTenantId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTenantId: %w", err)
	}
	return oldValue.TenantId, nil
}

// OldActorId returns the old "actor_id" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldActorId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldActorId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldActorId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldActorId: %w", err)
	}
	return oldValue.ActorId, nil
}

// OldMethod returns the old "method" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldMethod(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldMethod is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldMethod requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldMethod: %w", err)
	}
	return oldValue.Method, nil
}

// OldBy returns the old "by" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldBy(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldBy is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldBy requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldBy: %w", err)
	}
	return oldValue.By, nil
}

// OldObjectId returns the old "object_id" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldObjectId(ctx context.Context) (v uuid.UUID, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldObjectId is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldObjectId requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldObjectId: %w", err)
	}
	return oldValue.ObjectId, nil
}

// OldPatch returns the old "patch" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldPatch(ctx context.Context) (v []byte, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldPatch is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldPatch requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPatch: %w", err)
	}
	return oldValue.Patch, nil
}

// OldDateCreated returns the old "date_created" field's value of the Outbox entity.
// If the Outbox object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *OutboxMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *OutboxMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case outbox.FieldTenantId:
		return m.OldTenantId(ctx)
	case outbox.FieldActorId:
		return m.OldActorId(ctx)
	case outbox.FieldMethod:
		return m.OldMethod(ctx)
	case outbox.FieldBy:
		return m.OldBy(ctx)
	case outbox.FieldObjectId:
		return m.OldObjectId(ctx)
	case outbox.FieldPatch:
		return m.OldPatch(ctx)
	case outbox.FieldDateCreated:
		return m.OldDateCreated(ctx)
	}
	return nil, fmt.Errorf("unknown Outbox field %s", name)
}

// RepositoryMutation represents an operation that mutates the Repository nodes in the graph.
type RepositoryMutation struct {
	repository.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Repository, error)
}

var _ ent.Mutation = (*RepositoryMutation)(nil)

// repositoryOption allows management of the mutation configuration using functional options.
type repositoryOption func(*RepositoryMutation)

// newRepositoryMutation creates new mutation for the Repository entity.
func newRepositoryMutation(c config, op Op, opts ...repositoryOption) *RepositoryMutation {
	m := &RepositoryMutation{
		Mutation: *repository.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Repository entities.
func (m *RepositoryMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *RepositoryMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withRepositoryId sets the Id field of the mutation.
func withRepositoryId(id uuid.UUID) repositoryOption {
	return func(m *RepositoryMutation) {
		var (
			err   error
			once  sync.Once
			value *Repository
		)
		m.oldValue = func(ctx context.Context) (*Repository, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Repository.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withRepository sets the old Repository of the mutation.
func withRepository(node *Repository) repositoryOption {
	return func(m *RepositoryMutation) {
		m.oldValue = func(context.Context) (*Repository, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m RepositoryMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m RepositoryMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *RepositoryMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Repository.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldName returns the old "name" field's value of the Repository entity.
// If the Repository object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RepositoryMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldName requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// OldDesc returns the old "desc" field's value of the Repository entity.
// If the Repository object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RepositoryMutation) OldDesc(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDesc is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDesc requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDesc: %w", err)
	}
	return oldValue.Desc, nil
}

// OldDateUpdated returns the old "date_updated" field's value of the Repository entity.
// If the Repository object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RepositoryMutation) OldDateUpdated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateUpdated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateUpdated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateUpdated: %w", err)
	}
	return oldValue.DateUpdated, nil
}

// OldDateCreated returns the old "date_created" field's value of the Repository entity.
// If the Repository object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RepositoryMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *RepositoryMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case repository.FieldName:
		return m.OldName(ctx)
	case repository.FieldDesc:
		return m.OldDesc(ctx)
	case repository.FieldDateUpdated:
		return m.OldDateUpdated(ctx)
	case repository.FieldDateCreated:
		return m.OldDateCreated(ctx)
	}
	return nil, fmt.Errorf("unknown Repository field %s", name)
}

// TagMutation represents an operation that mutates the Tag nodes in the graph.
type TagMutation struct {
	tag.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Tag, error)
}

var _ ent.Mutation = (*TagMutation)(nil)

// tagOption allows management of the mutation configuration using functional options.
type tagOption func(*TagMutation)

// newTagMutation creates new mutation for the Tag entity.
func newTagMutation(c config, op Op, opts ...tagOption) *TagMutation {
	m := &TagMutation{
		Mutation: *tag.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Tag entities.
func (m *TagMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *TagMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withTagId sets the Id field of the mutation.
func withTagId(id uuid.UUID) tagOption {
	return func(m *TagMutation) {
		var (
			err   error
			once  sync.Once
			value *Tag
		)
		m.oldValue = func(ctx context.Context) (*Tag, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Tag.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withTag sets the old Tag of the mutation.
func withTag(node *Tag) tagOption {
	return func(m *TagMutation) {
		m.oldValue = func(context.Context) (*Tag, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m TagMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m TagMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *TagMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Tag.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldName returns the old "name" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldName requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// OldRepo returns the old "repo" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldRepo(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldRepo is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldRepo requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRepo: %w", err)
	}
	return oldValue.Repo, nil
}

// OldDigest returns the old "digest" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldDigest(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDigest is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDigest requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDigest: %w", err)
	}
	return oldValue.Digest, nil
}

// OldDateMoved returns the old "date_moved" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldDateMoved(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateMoved is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateMoved requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateMoved: %w", err)
	}
	return oldValue.DateMoved, nil
}

// OldDatePulled returns the old "date_pulled" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldDatePulled(ctx context.Context) (v *time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDatePulled is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDatePulled requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDatePulled: %w", err)
	}
	return oldValue.DatePulled, nil
}

// OldDateUpdated returns the old "date_updated" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldDateUpdated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateUpdated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateUpdated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateUpdated: %w", err)
	}
	return oldValue.DateUpdated, nil
}

// OldDateCreated returns the old "date_created" field's value of the Tag entity.
// If the Tag object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TagMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *TagMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case tag.FieldName:
		return m.OldName(ctx)
	case tag.FieldRepo:
		return m.OldRepo(ctx)
	case tag.FieldDigest:
		return m.OldDigest(ctx)
	case tag.FieldDateMoved:
		return m.OldDateMoved(ctx)
	case tag.FieldDatePulled:
		return m.OldDatePulled(ctx)
	case tag.FieldDateUpdated:
		return m.OldDateUpdated(ctx)
	case tag.FieldDateCreated:
		return m.OldDateCreated(ctx)
	}
	return nil, fmt.Errorf("unknown Tag field %s", name)
}

// TenantMutation represents an operation that mutates the Tenant nodes in the graph.
type TenantMutation struct {
	tenant.Mutation
	config
	id       *uuid.UUID
	done     bool
	oldValue func(context.Context) (*Tenant, error)
}

var _ ent.Mutation = (*TenantMutation)(nil)

// tenantOption allows management of the mutation configuration using functional options.
type tenantOption func(*TenantMutation)

// newTenantMutation creates new mutation for the Tenant entity.
func newTenantMutation(c config, op Op, opts ...tenantOption) *TenantMutation {
	m := &TenantMutation{
		Mutation: *tenant.NewMutation(op),
		config:   c,
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// SetId sets the value of the id field. Note that this
// operation is only accepted on creation of Tenant entities.
func (m *TenantMutation) SetId(id uuid.UUID) {
	m.id = &id
}

// Id returns the Id value in the mutation. Note that the Id is only available
// if it was provided to the builder or after it was returned from the database.
func (m *TenantMutation) Id() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// withTenantId sets the Id field of the mutation.
func withTenantId(id uuid.UUID) tenantOption {
	return func(m *TenantMutation) {
		var (
			err   error
			once  sync.Once
			value *Tenant
		)
		m.oldValue = func(ctx context.Context) (*Tenant, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Tenant.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withTenant sets the old Tenant of the mutation.
func withTenant(node *Tenant) tenantOption {
	return func(m *TenantMutation) {
		m.oldValue = func(context.Context) (*Tenant, error) {
			return node, nil
		}
		m.id = &node.Id
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m TenantMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m TenantMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// Ids queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *TenantMutation) Ids(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.Op().Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.Id()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.Op().Is(OpUpdate | OpDelete):
		return m.Client().Tenant.Query().Where(m.Predicates()...).Ids(ctx)
	default:
		return nil, fmt.Errorf("Ids is not allowed on %s operations", m.Op())
	}
}

// OldAlias returns the old "alias" field's value of the Tenant entity.
// If the Tenant object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TenantMutation) OldAlias(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldAlias is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldAlias requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAlias: %w", err)
	}
	return oldValue.Alias, nil
}

// OldName returns the old "name" field's value of the Tenant entity.
// If the Tenant object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TenantMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldName requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// OldDesc returns the old "desc" field's value of the Tenant entity.
// If the Tenant object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TenantMutation) OldDesc(ctx context.Context) (v string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDesc is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDesc requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDesc: %w", err)
	}
	return oldValue.Desc, nil
}

// OldLabels returns the old "labels" field's value of the Tenant entity.
// If the Tenant object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TenantMutation) OldLabels(ctx context.Context) (v map[string]string, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldLabels is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldLabels requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldLabels: %w", err)
	}
	return oldValue.Labels, nil
}

// OldDateUpdated returns the old "date_updated" field's value of the Tenant entity.
// If the Tenant object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TenantMutation) OldDateUpdated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateUpdated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateUpdated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateUpdated: %w", err)
	}
	return oldValue.DateUpdated, nil
}

// OldDateCreated returns the old "date_created" field's value of the Tenant entity.
// If the Tenant object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TenantMutation) OldDateCreated(ctx context.Context) (v time.Time, err error) {
	if !m.Op().Is(OpUpdateOne) {
		return v, errors.New("OldDateCreated is only allowed on UpdateOne operations")
	}
	if _, exists := m.Id(); !exists || m.oldValue == nil {
		return v, errors.New("OldDateCreated requires an Id field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDateCreated: %w", err)
	}
	return oldValue.DateCreated, nil
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *TenantMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case tenant.FieldAlias:
		return m.OldAlias(ctx)
	case tenant.FieldName:
		return m.OldName(ctx)
	case tenant.FieldDesc:
		return m.OldDesc(ctx)
	case tenant.FieldLabels:
		return m.OldLabels(ctx)
	case tenant.FieldDateUpdated:
		return m.OldDateUpdated(ctx)
	case tenant.FieldDateCreated:
		return m.OldDateCreated(ctx)
	}
	return nil, fmt.Errorf("unknown Tenant field %s", name)
}
